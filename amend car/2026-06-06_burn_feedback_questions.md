# 2026-06-06 烧录后反馈问题清单

本文件用于记录第一次把 `amend car/2026-06-06_2026v1_first_version/2026v1.c` 烧录到实体机器人后的反馈问题。请先按问题观察和回答；答案回来后，再统一写入修改日志并指导下一轮优化。

## 1. VSCode 编译与下载

- VSCode 打开的是不是 `amend car/2026-06-06_2026v1_first_version` 这个文件夹？
- 构建入口是不是选择 `2026v1.c`？
- 编译是否成功生成 `2026v1.bin`？
- 如果编译失败，完整报错里最关键的 3-5 行是什么？
- 下载时 E7-RCU 磁盘是否能被识别？
- `.bin` 文件是否成功复制到硬件盘？

回答：

- 已能烧录进硬件。
- 已能编译生成 `2026v1.bin`。
- 前面已修复过 `C++ GDB a.exe`、缺少 `arm-none-eabi-gcc.exe`、路径空格导致 GCC 拆路径等问题。

## 2. 上电和初始化

- 烧录后上电，屏幕是否有显示？
- 屏幕显示内容是否为 `Running` 或类似运行状态？
- 是否出现黑屏、卡死、蜂鸣异常、反复重启？
- 初始化阶段机械臂是否有明显乱动或撞车体？
- 夹爪初始状态是打开、半开还是夹紧？

回答：

- 屏幕出现中文编码乱码。
- 数值显示区可见，`X/Y/Z` 一类 ASCII 字符显示正常。
- 判断：设备端显示接口更适合 ASCII 或原工程本地编码；当前副本源码转 UTF-8 后，中文字符串会乱码。
- 处理：下一版将触摸界面和阶段显示改成 ASCII。

## 3. 底盘电机方向

- M1、M2、M3、M4 的实际位置是否确认？分别对应前左、前右、后左、后右吗？
- 机器人执行前进时，实际是前进、后退、横移、旋转，还是某个轮子反向？
- 执行后退时是否方向正确？
- 执行左移/右移时是否方向正确？
- 是否有某个电机不转、转速明显不同或声音异常？

回答：

- 动作实际和预计基本一致。
- 轮子没有明显问题。
- 当前判断：M1-M4 电机方向和底盘运动映射第一轮可先视为通过。

## 4. 距离和旋转标定

- `go_bmp()` 直线运动距离是否明显偏大或偏小？大概偏差多少？
- 横移距离是否明显偏大或偏小？大概偏差多少？
- `turn_o()` 旋转角度是否明显偏大或偏小？例如目标 90 度实际转了多少？
- 运动结束后是否能稳定停住，还是会冲过头/抖动？

回答：

- 暂未反馈具体距离或角度偏差。
- 从现场描述看，动作方向符合预期，后续再做量化标定。

## 5. AprilTag 视觉对位

- AI 摄像头是否能识别场地 AprilTag？
- 进入 `go_to_tag()` 时机器人是否会朝标签方向修正？
- 标签看不到时，机器人是否会在超时后停下，而不是一直卡死？
- 对位完成后，车身位置和角度是否基本可用？
- 光照、距离、角度是否会明显影响识别？

回答：

- 跑到识别 A2/A3 图或 Tag 阶段时出现问题。
- 需要下一版增加英文阶段提示，区分到底是 `TAG A2` 失败还是 `TAG A3` 失败。
- 需要继续确认：失败时车是停住、原地微动、乱跑，还是超时后继续下一步。

## 6. 机械臂与夹爪

- 夹爪打开角度 `ROBOT_PAW_OPEN = 1100` 是否足够打开？
- 夹爪夹紧角度 `ROBOT_PAW_GRIP = 1700` 是否能夹住方块？
- 夹紧是否过紧，有没有挤坏方块或卡舵机？
- 机械臂执行抓取姿态时是否能到达目标高度？
- 放置姿态是否会碰撞车体、场地或任务道具？

回答：

- 机械臂有点问题，但还需要具体描述。
- 待确认问题类型：高度不对、角度不对、夹爪开合不对、碰撞车体、碰撞道具、抓不到、放不下，还是动作太快/太慢。

## 7. 基准路线整体表现

- 程序开始后，机器人第一段动作是否安全？
- 是否能跑到第一个 AprilTag 附近？
- 哪一步开始明显偏离路线？
- 是否发生撞墙、撞道具、越界、卡住或机械臂刮擦？
- 整体路线执行到哪里停止或失败？

回答：

- 机器人动作实际和预计没问题。
- 轮子没出问题。
- 路线执行到识别 A2/A3 图或 Tag 阶段开始出问题。

## 8. 最需要优先修的问题

- 你观察到的最大问题是什么？
- 是编译/烧录问题、底盘方向问题、距离标定问题、视觉问题、机械臂问题，还是路线策略问题？
- 如果只能先修一个点，你希望先修哪一个？

回答：

- 当前优先级建议：
- 第一优先：修复屏幕中文乱码，改成 ASCII 阶段显示。
- 第二优先：给 A2/A3 识别阶段加明确英文状态，例如 `TAG A2`、`TAG A3`、`TAG OK`、`TAG FAIL`。
- 第三优先：等机械臂问题具体化后再调姿态参数，避免盲改。
